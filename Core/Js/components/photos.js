'use strict';

// Import styles and js for the photo gallery here.
// They will be part of separate css/js file and not load on every page.

export default (photoGallerySelector = '.photo-gallery') => {
    console.log('Loaded photo gallery!');
};
